# Archive

This directory keeps retired code in the repository without exposing it as the
mainline.

```text
stage1/
  Changing-resolution Stage 1 residual clean-resizer model, configs, scripts,
  and old training docs.

v1/
  Early noisy-to-clean V1 scripts, configs, and model code.
```

The current mainline imports and wrappers do not depend on this directory.
